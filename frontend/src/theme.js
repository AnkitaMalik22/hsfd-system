import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#ff3d00",
      light: "#ff6333",
    },
    secondary: {
      main: "#b22a00",
    },
    otherColor: {
      main: "#999",
    },
  },
});
